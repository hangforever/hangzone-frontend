{
  "name": "functions",
  "scripts": {
    "lint": "./node_modules/.bin/tslint --project ./tsconfig.json",
    "watch": "yarn lint && tsc -w",
    "build": "yarn lint && tsc",
    "serve:all": "concurrently --kill-others 'yarn watch' 'firebase emulators:start'",
    "serve": "concurrently --kill-others 'yarn watch' 'firebase emulators:start --only functions'",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "docs:api": "docker-compose up swagger-redoc"
  },
  "engines": {
    "node": "12"
  },
  "main": "lib/index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase-admin": "^9.3.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.8",
    "@types/testing-library__dom": "^7.5.0",
    "concurrently": "^5.3.0",
    "firebase-functions-test": "^0.1.6",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "private": true
}
